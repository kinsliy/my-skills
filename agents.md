# 全局工作上下文（Global Working Context）

> 本文件为 AI 协作时必须遵守的全局规则。任何任务开始前，请先按本文件章节顺序执行，遇到冲突时以「优先级最高」标记的条目为准。
>

必须中文回复我。

## 模块 A — 最高优先级（P0 总览）

## 禁止根据没有任何事实依据的猜测，直接修改代码，或者当做结论，任何事情都需要有事实依据，除非用户已确认了。

## 任何任务如果遇到没有直接可以使用的工具时，需要反馈用户是不是有对应的工具可以使用，或者建议直接做一个，方便后续任务不卡住，AI 直接能跑完。

### A.1 P0-2 最小改动
修改原有逻辑时遵循最小化改动原则，禁止过度优化；原有变量/函数名一律保留，不重命名；新增代码必须遵守 模块 D 代码规范。


### A.3 P0-4 澄清优先（Clarify First）
模糊或信息不全的需求或者要求**禁止凭猜测开工**，必须先提问澄清。审查 / 评估 / 对比类任务采用 **Disprove Mode（反证模式）**，1:1 checkbox 比对，举证而非陈述。


### A.5 P0-6  在开始执行任何任务前，必须确认你已理解完成该任务的必要且准确的上下文信息。
- 若信息不完整或存在歧义，通过提问工具主动向用户确认，不得自行假设或填补缺失信息。
- 提问需聚焦关键信息，特别关注：需求边界、边界场景（异常值、空值、极端情况）、输入输出约定。
- 每次只提一个问题，按优先级逐步确认，直到信息充分后再开始执行。
禁止行为：不得在信息不完整时直接开始执行，也不得用"我假设…"代替真实确认。


### A.7 P0-8 目标驱动执行（Outcome-Driven）
动手前必须先写出**可验证成功标准**：
- 「修 bug」→ 先给复现路径 / 复现脚本；**复杂 bug 强制先写复现测试再修**；琐碎 bug（见模块 F 豁免）可口头复现即可。
- 「加功能」→ 先列 ≥1 条可跑通的验证用例（手测步骤 / 接口调用 / 单测命令）。
- 「重构」→ 必须先确认重构前的行为基线（已有测试 / 手测清单），重构后必须等价通过。
- 无可验证标准 ⇒ 视为模糊需求，回到 P0-4 澄清。

### A.8 P0-9 反过度工程（Anti-Over-Engineering）
**新增代码同样适用**，不得以"新增不受最小化约束"为豁免理由：
- 禁止臆造灵活性：不为不存在的需求加配置项 / 抽象层 / 插件点。
- 禁止过度防御：仅在系统边界（用户输入、外部 API）做校验，框架/内部已保证的不变量不重复校验。
- **3 处相似代码再抽象**：1~2 处复制可接受，第 3 处出现时再提取公共函数。
- 提交前自检："资深工程师会觉得过于复杂吗？"答"是"则删减。

**变更留痕**: 所有非琐碎代码改动（含修复 bug、调整样式、修改逻辑）必须在改动点附近以单行注释留痕，格式：`// 变更留痕：YYYY-MM-DD HH:MM 北京时区；原因：xxx；背景：xxx`。时间必须真实准确（取当下时间，不得编造）；仅在主入口保留一处，避免散落。

排查/调试 aPaaS 云函数或要看其运行日志时，可以调用 skill `apaas-function-debug`，禁止凭印象敲 ae 命令。

## 模块 B — 任务流程

### B.1 澄清前置（Clarify First）
1. 评估清晰度：目标 / 范围 / 禁止项 / 边界 / 产物 / 验收标准，任一缺失即视为模糊需求，必须提问对齐。
2. **方案权衡明示**：存在 ≥2 种合理实现时禁止默默选解，必须列权衡矩阵（方案 / 优点 / 风险 / 工作量）由用户拍板。




### B.4 Skill 优先
- 任务开始前先扫描 `available_skills`，存在即优先调用。
- 涉及自定义组件 / 不熟悉 API，先查官方文档或向用户咨询，禁止凭印象写。

---

## 模块 C — 改动原则

### C.1 改动原则表

| 场景 | 原则 |
| --- | --- |
| 修改原有逻辑 | 最小化改动，不重构、不重命名、不优化无关代码 |
| 大范围改动 | 禁止直接覆盖旧代码，以新增函数/逻辑增量介入 |
| 新增逻辑 | 模块化 + 函数式风格；同时遵守 P0-9 反过度工程 |

### C.2 孤儿代码清理
- 改动产生的**无用 import / 局部变量 / 私有函数 / 私有类型**必须同次提交一并删除。
- 预先存在、与本次改动无关的死代码**不主动删**，仅在对话中提醒用户。

---

## 模块 D — 代码规范

### D.1 命名风格（仅适用于新增代码）
- 全部使用 `snake_case`。
- 变量名要求**详细具名**，禁止缩写（`usr` ❌ → `current_user_info` ✅）。
- 原有变量名保持不变。

### D.2 编程范式
- **函数式编程思想**，使用数据驱动思想。
- **模块化**：独立逻辑必须拆分为独立函数，函数内部继续按职责拆解为子函数，主函数仅做编排。
- 拆分目标：可替换、可拆分、易测试。

### D.3 健壮性与安全
- 对象取值统一使用 **可选链运算符 `?.`**，避免空指针。
- 兼顾性能与安全，禁止引入 bug。
- 防御性校验只在系统边界（见 P0-9）。

### D.4 复用与依赖
- 编码前先检查是否已有同类实现，避免重复造轮子。
- 前端项目优先使用 **`ahooks`** 或其他成熟库，禁止手写已有库提供的能力。

### D.5 函数复杂度（Clean Code 原教旨）
- **函数长度 ≤20 行**：超过必须按职责拆子函数；主函数仅做编排。
- **参数 ≤3 个**：超过 3 个必须聚合为对象传参（`{ user_id, order_id, amount }`）。
- **嵌套深度 ≤3 层**：超过必须用 **early return / guard clause** 拍平。

  ```js
  // ❌ 嵌套地狱

  // ✅ early return




## 模块 E — 注释与日志

### E.1 注释

**优先级**（按高到低写，what 仅在白名单内允许）：
1. **why** — 业务背景 / 意图（必写）
2. **权衡** — 为什么不用方案 B（有备选时必写）
3. **坑点** — 上游字段坑、SDK bug、兼容 workaround（命中即写）
4. **what 白名单** — 仅 3 类允许：领域概念翻译 / 复杂算法或正则 / 长函数 Step 锚点

**禁止**：代码自证型（`i++ // i 加 1`）、同义重复（函数名已表意还翻译）、死代码注释、无责任人无背景的 TODO。

**自检三连**（任一答否 → 删）：
1. 删掉它，读者会困惑吗？
2. 内容能从命名/结构读出吗？能 → 多余。
3. 命中 why / 权衡 / 坑点 / 白名单 之一吗？

**示例**：
```js
i++;                              // ❌ 自增（自证）
i++;                              // ✅ 跳过表头（why）
const previous_ref = useRef(v);   // ✅ 用 ref 避免重渲染（权衡）
record?.i18n?.[0]                 // ✅ 后端偶发 null，jira-1234（坑点）
// 劳务顾问：ID 命中 system env  // ✅ 领域翻译（what 白名单）
```



### E.3 Lint
- 不需要处理 lint 问题，可忽略。

---

## 模块 F — 琐碎改动豁免

满足**全部**条件视为琐碎改动，可跳过 TDD / 计划签字 / 权衡矩阵：
- 单文件
- ≤30 行净增量
- 不引入新依赖 / 不动公共逻辑
- 复现路径口头能说清

仍需遵守：P0-2 / P0-3 / P0-7  / 模块 D 代码规范。




### 关键卡点
- **澄清优先**：信息不全先提问，禁止猜测推进。
- **写文档前必须主动提问**：列出信息缺口清单 → 等用户确认 → 再下笔。
- **编码前必须先思考并列出 todolist**，再动手。
- 每完成一个 todo 立即标记 completed，不批量结算。
- **审计/Review 类任务**：主文档 + `*_conclusion.md` 双产物，缺一不可。